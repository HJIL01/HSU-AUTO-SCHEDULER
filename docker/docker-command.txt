\dt: 현재 데이터베이스의 테이블들을 보여줌

docker exec -it hsu_auto_scheduler_postgres psql -U dba1 -d hsu_auto_scheduler: 도커를 활성화 시키고 dba1 계정으로 hsu_auto_scheduler라는 데베에 접속하라

bash 접속 상태에서 \i sql/ddl/01_create_semester_table.sql: 해당 파일을 실행하라

docker compose down: 도커 컨테이너 삭제해라
docker compose down -v: 볼륨까지 삭제해라

docker compose up -d: 도커 생성해라

docker volume ls: 모든 볼륨 확인